# ===== RAHASIA & DATA SENSITIF (JANGAN PERNAH DI-PUSH) =====
# Daftar akun berisi password asli — gunakan akungsuite.example.txt sebagai template
akungsuite.txt

# Konfigurasi lokal (path Chrome utama per perangkat) — gunakan config.example.json sebagai template
config.json

# Profil Chrome kustom: 1,8 GB data sesi/cookies/login — bisa dibuat ulang via `python sync.py prepare`
profiles/

# ===== CACHE & TEMPORARY =====
__pycache__/
*.pyc
*.pyo

# Backup otomatis dari sync.py
.backup-sync-*/
.backup-manual-*/
